# Ops Log - 2026-03-05

## 范围

- 20节点 OSPF 动态测试
- 动态拓扑性能基准
- SRv6 临时改路控制闭环模拟
- SRv6 内核协议 canary

## 关键脚本

- `scripts/test_suite_20node_vlan_subif.sh`
- `scripts/test_suite_20node_dynamic_all.sh`
- `scripts/benchmark_dynamic_topo_20.sh`
- `scripts/test_srv6_temp_reroute_20.sh`
- `scripts/test_srv6_kernel_canary_20.sh`

## 关键结果

1. 20节点全量回归通过。
2. 动态扰动（QoS/通断/时延/单播/广播/组播）通过。
3. 动态拓扑基准（10轮）指标：
   - `T_apply_down` P50=222.5ms
   - `T_routes_ok_down` P50=4514ms
   - `T_recover_up` P50=221ms
   - `T_full_converge` P50=11033ms
4. SRv6 临时改路模拟闭环可执行。
5. SRv6 内核 canary 最终打通：
   - 加策略后 `ping6 0% loss`
   - 抓到 mid 入口 SRH 与 mid->dst decap 后业务包

## 关键问题与修复

1. `iperf3` 启动竞态与解析问题：
   - 增加重试与守护启动；
   - 修复吞吐解析列。
2. 广播/组播验证不稳定：
   - 广播改 ARP 抓包验证；
   - 组播改 OSPF Hello 抓包验证。
3. SRv6 子接口未生效：
   - 对 `veth_0.<vid>` 改用 `/proc/sys/net/ipv6/conf/<iface>/seg6_enabled` 开启。

## 对后续阶段的意义

- 已具备“故障事件 -> 临时改路 -> 业务维持 -> 回切”的技术基础。
- 可以在此基础上推进 50/100/300 节点分阶段 SRv6 动态策略验证。

